import React from 'react';

function Profile() {
    return (
        <div>
            {/* "profile" includes your questions that have been answered */}
            {/* if you have a question that you're waiting for an answer, renders that */}
            {/* otherwise, renders the option to ask another question */}
        </div>
    );
}

export default Profile;